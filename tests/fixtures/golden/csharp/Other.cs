namespace Golden.Other
{
    // Decoy: same class and method names as `Golden.Helpers.Helper`, and
    // a same-named `LocalUtil`.
    public static class Helper
    {
        public static string FormatGreeting(string name) => "Other";

        public static string LocalUtil() => "other";
    }
}
