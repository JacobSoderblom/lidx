def format_greeting(name: str) -> str:
    """Imported by name into caller.py (`from helper import format_greeting`)
    and called bare — the classic `from x import y` call shape."""
    return f"Hi, {name}"
